{
  "name": "midmem-kb-store",
  "version": "0.1.0",
  "private": true,
  "description": "Modular middleware layer bridging OpenClaw + Hermes with knowledge store, actions, history, logs, and long-term memory",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm --prefix packages/core test",
    "verify": "npm --prefix packages/core run verify && node scripts/gen-docs.mjs --check",
    "mcp:server": "node packages/core/bin/mcp-server.mjs",
    "cli": "node packages/core/bin/cli.mjs"
  },
  "engines": {
    "node": ">=22.13.0"
  }
}
